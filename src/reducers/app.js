export const initialState = {
  listPopular: [],
  listLastest: [],
}

const app = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state
  }
}

export default app